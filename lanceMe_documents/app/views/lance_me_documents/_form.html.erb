<%= form_for(lance_me_document) do |f| %>
  <% if lance_me_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lance_me_document.errors.count, "error") %> prohibited this lance_me_document from being saved:</h2>

      <ul>
      <% lance_me_document.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :file_url %>
    <%= f.text_area :file_url %>
  </div>

  <div class="field">
    <%= f.label :image_url %>
    <%= f.text_area :image_url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
